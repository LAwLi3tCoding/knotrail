# Knotrail：机会探索与产品取舍 v3.1

> 交付更新（2026-09-06）：可运行的 v0.1.0 已实现。本文保留设计与研究背景，当前能力、限制和验收以 [README](README.md)、[实现架构](ARCHITECTURE.md) 与 [验证报告](docs/VALIDATION.md) 为准。
状态：待验证机会，2026-09-05。本文取代旧版 48 个方向和主观加权评分，不再用底座能力数量代替产品价值。主方案见 [PRODUCT-BRIEF.md](PRODUCT-BRIEF.md)，竞品事实见 [COMPARISON.md](COMPARISON.md)。

## 1. 亮点应当发生在用户需要介入的时候

“会规划”“可以画图”“节点可展开”“任务能恢复”都已有实现。Knotrail 应重点解决一个连贯的实际问题：**工作做到一半，要求或项目变了，我怎样判断哪些成果还能用，并让它正确接着做？**

这类场景同时需要计划、执行过程与产物视图，能把用户希望的前端体验变成可操作能力。用户不必为了看进度读一遍完整会话，也不应为了改一项要求从头重跑。

这不是已验证的市场。我们有本项目的明确使用偏好，以及个别外部需求信号：Kiro 用户在 [issue #9435](https://github.com/kirodotdev/Kiro/issues/9435) 描述了计划完成后延迟数天实施、代码已改变、只能人工核对相关文件的麻烦，并请求记录 gitRef 和恢复时检查漂移。该案例支持问题值得测试，不能推导行业普遍需求或最新版产品没有替代能力。

## 2. 十个具体方向与优先级

P0 是首版三项主打能力；P1 仅在真实样本反复出现时补充；P2 为后续受限实验；“不做”不进入当前产品路线。下面的收益均为假设。

| 优先级 | 具体能力 | 用户会怎样用 | 最强现有替代与剩余验证点 |
| --- | --- | --- | --- |
| **P0-1** | **改计划时预览影响并安排局部继续** | “这个接口仍要兼容旧配置。”App 展示新增要求、受影响节点、需重新验证的产物和仍可复用的调研 | Kiro Refine/Sync Files、Cursor 修改计划后重新执行；需证明我们少重述、少误复用、少无效重做 |
| **P0-2** | **从节点直接查看当前有效的产物与验证** | “适配这一步为什么是完成？”点开看到 diff、输入版本、检查结果、缺口和历次尝试 | Devin 步骤详情、Kiro 测试关联、Kiro＋Karate 验证；需证明查证更快，输入改变后旧验证正确失效 |
| **P0-3** | **中断或隔天回来，先核对成果再继续** | 恢复任务时看到项目变化、未确认动作、过期依据和下一项决定；先查证再继续 | OpenClaw Workboard/Task Flow、框架 checkpoint、现有会话恢复；需证明不重复未知动作且接管时间更少 |
| **P1-1** | 决策点直接展示代价和受影响工作 | 两种兼容方式需要选择时，显示差异、改动范围和需要补的检查；用户针对该决定作答 | 现有计划讨论已可分析取舍；机会在决定与后续节点长期对应，避免选择被后续运行遗忘 |
| **P1-2** | 临时插入调查步骤并保留计划偏离记录 | 测试暴露未知行为时，插入“确认行为来源”节点，记录为何暂停实施和继续条件 | 动态工作流与 Devin 计划调整已有；需验证不会静默偏离目标，也不会每次小发现都打断用户 |
| **P1-3** | 人工接手后给出准确的继续边界 | 用户修正一个文件，再交回 Agent；App 核对哪些输入和检查受影响，保留人工修改 | Devin 可暂停接管，Kiro 有 checkpoint；机会是减少交接说明并避免恢复操作覆盖人工成果 |
| **P1-4** | 完成功能后生成按步骤组织的审阅材料 | 审阅者从“接口→页面→测试”查看相关 diff、关键决定和验证，不必先读全量工具日志 | 现有 PR 描述、代码审阅和测试报告；需验证比普通 diff 导航更快，不能多生成一份无人读的报告 |
| **P2-1** | 判断曾经成立的项目结论何时需要重查 | 计划引用旧接口约定时，提示依据来自哪个版本；依赖变化后重新核对而非静默沿用 | 项目记忆、检索与版本工具已有；先只覆盖明确引用和输入，不建立万能知识图谱 |
| **P2-2** | 从一项失败节点提取可复现的诊断样本 | 相同输入多次失败时，保存受控输入、环境说明、错误和检查，便于修 runtime 或工具 | tracing/eval、现有调试框架已有；只在实际失败上使用，验证能否更快定位而非多存日志 |
| **不做** | 为每个任务竞赛多个 Agent，再自动挑“最好”的结果 | 看起来更自主，但增加成本、合并冲突与判断负担 | Cursor 已有多 Agent judging，其他框架也支持并行；首版没有证据表明单 Agent 是瓶颈 |

这些方向不是十套系统。P0 共用计划版本、节点 attempt、产物、检查和当前输入；P1 主要改进这些对象的交互。只有真实任务需要时才增加状态或扩展点。

## 3. 三项主打能力怎样形成一个可理解的产品

### 3.1 计划可以改

先计划是固定工作过程，计划内容可以修改。小任务一节点，复杂任务默认 3—7 个工作节点。可选规划侧栏展示持续记录的事实、约束、疑问、方案摘要和草稿变化；关闭只隐藏界面，不跳过规划或丢失待决问题；Ready 后在已有授权内自动执行，也支持用户选择先审阅。

首次计划是一个可检查的提案。执行中发现新事实，或者用户改变要求，App 展示修订原因和影响，再保存新计划版本。修改目标或授权边界还必须生成任务版本，不能用“优化计划”自动扩大范围。

这一方向的演示必须出现真实的中途要求变更。只从空白生成一个漂亮流程图，不足以证明价值。已有 [Cursor 流式 Mermaid](https://cursor.com/changelog/2-2) 与 [Kiro Specs 迭代](https://kiro.dev/docs/specs/best-practices/) 是最低比较基线。

### 3.2 过程看得清

图和列表读取同一份计划数据，选中任何节点都能看到其目标、输入、依据摘要、实际动作、产物、验证与历次 attempt。高层节点对应一段有产出的工作，工具调用是节点详情中的执行记录。

完成信号必须区分“生成了产物”和“已按当前条件检查”。检查通过也必须说明覆盖范围。测试输出、截图或报告只能支持它实际验证的部分，不能自动证明全部语义正确。

Devin 已提供统一的步骤活动和命令输出视图；Kiro 属性测试也能关联需求、任务和代码。我们的验证点是这些信息能否在需求变化后仍保持一致，以及用户是否更快判断当前状态。[Devin](https://docs.devin.ai/work-with-devin/devin-session-tools)、[Kiro Correctness](https://kiro.dev/docs/specs/correctness/)

### 3.3 已有成果接得上

“重做节点”首先意味着核对当前工作区、已有产物和相关输入，然后创建新 attempt。它不是任意 Git 回滚，更不是外部动作的逆操作。已知有效的结果可复用，关系不明的部分应复验，未知动作先查证。

首版按显式依赖和可核对输入判断影响。一个节点改了另一个节点共用的文件，就不能仅凭图上没有连线宣称互不影响；必要时扩大复验。依赖关系是模型建议并经应用校验的工作描述，不是完整的程序语义证明。

Kiro 的 checkpoint 文档明确列出 shell/MCP 修改和后续人工编辑的限制；框架的 time travel 也不等于恢复外部世界。我们必须展示自己的覆盖范围，不能用“无损继续”做绝对承诺。[Kiro Checkpoints](https://kiro.dev/docs/chat/checkpoints/)、[Mastra Time Travel](https://mastra.ai/docs/workflows/time-travel)

## 4. 用什么任务验证

| 场景 | 要发生的实际变化 | 要观察的能力 | 不应人为制造的工作 |
| --- | --- | --- | --- |
| 兼容依赖升级 | 测试阶段新增旧配置兼容要求，或调查发现真实接口取舍 | 计划修订、影响预览、产物复用、当前版本复验 | 不为了画图把简单版本替换拆成多轮，不反复制造相同错误 |
| 跨接口、页面、测试的小功能 | 修改一项已澄清交互或接口约束 | 节点产物关联、跨节点影响、审阅与交付 | 不扩成多仓、多角色团队和完整需求管理平台 |
| 同一任务中断后继续 | 用户暂停并修改相关代码，再恢复 | 观察变化、保留人工修改、查证未知动作、恢复时间 | 不用强制杀进程次数充当用户长期需求证明 |
| 文档示例持续维护，后续验证 | 项目接口发生真实变化 | 计划依据过期、重复委托、健康状态失效 | 不把文档维护包装成首版第二市场 |

每个自然任务记录人工分钟、重复说明、错误复用、无效重做、交付是否符合当前输入与条件。故障注入用于证明边界行为，不能代替自然任务的可用性评价。

## 5. 最容易误判为亮点的内容

| 看起来有亮点 | 为什么不足以支撑产品 | 正确位置 |
| --- | --- | --- |
| 有前端、有规划、有流程图 | Cursor、Kiro、Devin 及工作流工具已有 | 用户明确需要，首版必须做好，但属于体验基础 |
| Agent 一直运行或自动重试 | 长任务产品和任务框架已有，持续运行也可能持续犯错 | 有预算、等待与进度判断的执行机制 |
| 计划存 SQLite、有 revision | 工程实现选择，用户不会因此迁移 | 用来支持可追溯修改和正确失效 |
| 支持更多模型、MCP、插件 | 已是常见扩展能力 | 只接入真实任务需要的工具和模型 |
| 展示模型所有思考 | 无法等同于真实内部推理，也会淹没用户需要的信息 | 展示调查事实、方案摘要、决定及执行证据 |
| 自动总结经验并升级自己 | 既有技能迭代与评测工具已覆盖部分需求；自动修改规则有额外维护负担 | 重复出现真实案例后再做受控改进 |
| 一键回滚任意步骤 | 共享文件、人工修改和外部副作用不能随意逆转 | 首版只提供核对后的当前工作区重做 |

## 6. 开发取舍与继续条件

前端是本次用户明确指定的产品形态，从 M0 开始交付。它不是等后台成熟后才添加的外壳。CLI 只保留开发与诊断用途。完整五阶段粗估 28—44 工程日，首五日包含其中，先验证用户能否通过界面完成计划修改和节点查证。[实施计划](IMPLEMENTATION-PLAN.md)

产品优势必须与强替代对照：编码侧选择实际可用的 Codex、Cursor 或 Kiro 规划工作流；组合替代侧检查 OpenClaw Workboard、Mastra Factory，以及 Kiro 配合验证工具。不能把竞品限制为裸聊天，再宣布自己的图和检查更强。

继续扩建的条件是：真实任务中反复出现更少的计划解释、更快的状态查证、更少的无效重做，并保持当前交付正确性。若收益主要来自一小块验证或计划插件，就把该部分单独做扎实；若只有作者学习 pi 的收益，保留个人工作台也有意义，但不包装成已成立的行业优势。
