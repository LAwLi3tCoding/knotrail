# Knotrail：实施与验证计划 v3.1

> 交付更新（2026-09-06）：可运行的 v0.1.0 已实现。本文保留设计与研究背景，当前能力、限制和验收以 [README](README.md)、[实现架构](ARCHITECTURE.md) 与 [验证报告](docs/VALIDATION.md) 为准。
状态：2026-09-05，待实施。当前交付为方案和可交互界面示意，尚未实现正式应用、接入模型或运行以下实验。M0 从桌面开始，替代 v2 的 CLI 先行路线。

依据：[产品方案](PRODUCT-BRIEF.md)、[架构](ARCHITECTURE.md)、[界面设计](UI-DESIGN.md)、[竞品对比](COMPARISON.md)。源码学习见 [RUNTIME-GUIDE.md](RUNTIME-GUIDE.md)。

## 1. 首版要证明什么

用户提交目标后，在以 Codex 为主要参照、结合 DSH 与 Catdesk 布局的双语工作区推进任务。对话、活动、变更和工具面板可分别查看；右侧规划栏随时开关，展示计划形成过程与步骤依赖。执行期间点开每一步，看到输入、实际动作、产物和检查。需求中途变化时，能判断哪些结果仍有效、哪些需重查，并在当前工作区继续，减少重述和人工翻日志。

这是一款可以做日常开发的桌面 Agent。第一例选择有真实兼容取舍的依赖升级；第二例选择涉及接口、页面和测试的小功能。一次简单改动也必须先形成一节点计划，但不多加一道人工批准。有限托管与隔夜恢复随后进入同一套 Task，不另造一套“委托任务”状态。

工程日粗估 **28—44 日**，约一名工程师 6—9 个工作周。假设个人 macOS、单 Git 项目、一个可用 provider 和明确工具链，不含云服务、多系统兼容、商店分发和多人协作。各阶段包含最小必要检查；M1 后按实际隔离、SDK 和图交互风险重估。

一份 TypeScript 工程、Electron + React、SQLite 单写者、pi coding-agent SDK、一个活动效果执行器。CLI 仅诊断，不先做完整终端产品；图组件解决布局即可，不开发通用流程编排平台。

## 2. 前五天：先验证这套交互是否有用

探索预算包含在 M0/M1 中，不是五天交付正式 Agent 的承诺。当前示意只验证展示和交互；后续进入开发才产生真实运行证据。

| 次序 | 最小产物 | 判定 |
| --- | --- | --- |
| 第一天 | 固定真实依赖升级题目与成熟产品基线；在草图中完成“找下一步、看产物、改兼容要求” | 用户能理解何处看计划、何处看实际执行；若成熟产品已足够，记录其更小实现路径 |
| 第二天 | 双语桌面壳、对话 / 活动 / 变更视图、独立工具面板和右侧规划栏；一份任务数据驱动各视图 | 小屏规划为右侧抽屉；切语言保持状态和原文；所有模拟状态明确标注 |
| 第三天 | 计划草稿、Ready、执行、失效四类状态及节点详情 | 规划过程和最终计划有区别；用户不会把“写完”误读为“验收通过” |
| 第四至五天 | 同一案例的 v1→v2 影响预览、局部继续示意；最薄版本数据持久化 | 用户能指出保留/重查的节点和原因；重开界面不靠聊天恢复计划 |

若有余量，启动隔离环境中的 SDK 最小接线；不能为凑五天跳过 M1 的执行边界。模型未接入、影响范围手工指定或事件由计时器模拟时，原型与报告都明确说明。交互偏好不等于任务收益或市场需求。

## 3. 五个交付阶段

| 阶段 | 用户可见结果 | 最少新增内容 | 验收证据 | 工程日 |
| --- | --- | --- | --- | --- |
| **M0 桌面交互** | 双语工作区、右侧规划栏、活动 / 变更与工具面板、节点详情 | Electron + React 壳；单一 read model；文案与偏好；明确标记的场景 fixture | 真实用户完成三个交互问题；布局、语言、面板独立与键盘行为符合约定 | 3—5 |
| **M1 真实规划到执行** | 输入目标后真实查项目、生成计划，再逐节点执行 | SDK、只读 Planner、计划校验与发布、受限工具、Run/Task/Plan 持久化、宿主锁 | 实际依赖升级；Ready 前无项目效果；停止后无残留写 | 6—9 |
| **M2 节点证据与改计划** | 每步可看产物/检查；需求变化有影响预览 | 节点归属、版本引用、固定验收、v1→v2、选择性复用与保守失效 | 升级中改兼容要求；API/页面/测试小功能；旧结果不冒充当前通过 | 7—11 |
| **M3 局部继续与托管** | 从选定步骤继续；隔夜/重启后查证再工作 | 新 attempt、未知效果查证、有限等待、事件合并、预算、维护模式最小路径 | 不丢用户修改、不重放未知写；迟到事件、休眠、来源变化行为正确 | 7—11 |
| **M4 同题比较与交付** | 知道何种工作值得交给 Knotrail | 本地打包、任务导出、强基线比较与维护成本记录 | 实际任务报告、失败边界、用户继续使用意愿 | 5—8 |

合计 28—44 工程日。阶段串行收敛；只为已出现的数据与行为建模块。M2 的交互收益不足时先缩小能力，不用堆插件、多 Agent 或更多节点掩盖问题。

### M0：界面成为首版主入口

桌面主要布局和视觉沿用 Codex App，按 [界面设计](UI-DESIGN.md) 纳入 DSH 与本地 Catdesk 的实际参照：可折叠项目任务导航、紧凑顶部工具入口、对话 / 活动 / 变更视图、独立底部工具面板和分组设置。右侧规划栏单独开关，首次默认关闭并保存选择；过程视图放事实、问题、方案与修订，步骤视图放图 / 列表和节点详情。关闭后主区扩展，开关不影响规划执行、左侧导航或工具面板。主对话始终可处理必要决定。

先按 UI-DESIGN 中的真实截图核对窗口、导航、消息和输入框，再验收规划侧栏。布局不溢出或交互可用不能代替与 Codex 的视觉对照；用户指定版本截图优先于公开示意。

简体中文与 English 从 M0 纳入文案键和设置，正式应用默认跟随系统并保存手动偏好。检查导航、动态状态、错误、空状态与可访问名称；切换不改任务 / 计划 / 节点 / 事件 ID、历史原文、输入草稿和打开的面板，也不触发任务命令。模型回复语言单独配置，不随界面语言自动变更。

文件 / 终端 / 预览在 M0 只展示模拟材料；模型、权限、推理强度和上下文配置明确标记接入状态。M1 才接实际执行与输出，M2 才验证变更、产物与节点证据引用。不得因界面已有入口就宣称真实终端、浏览器、模型配置或插件市场可用。

同一份结构化数据驱动各视图，不从最终 Markdown 解析图再伪造过去的流式过程。图布局是显示状态，不能改变依赖或执行状态。先用少量语义节点；复杂连线实际需要时再接成熟图组件。

键盘可切换规划栏、遍历节点与按钮；关闭栏后焦点回到开关，隐藏内容不可获得焦点。状态不只用颜色；窄屏规划为右侧抽屉，不能排到对话下方，支持 Escape 关闭与焦点限制。关闭后不留空白，不强挤三栏。模拟数据不使用真实进度承诺；正式实现只展示实际事件与检查数量。

### M1：先规划后执行是真实协议

确认拟安装包与固定 pi 源码的导出一致，记录锁文件和来源。显式注入 ModelRuntime、SettingsManager、SessionManager、资源白名单；`noTools: "builtin"` 配合显式工具集，不能只传 customTools 而保留默认旁路。只接一种 provider，记录不支持的能力和用量缺口。

在本阶段为 Task(mode=once) 建立 TaskRevision、PlanningDraft、PlanRevision、Run。NodeAttempt 就是 purpose=node 的 Run，不建平行状态机。Coordinator 唯一写库；所有应用入口获得同一 OS owner lock。旧 helper 未确认停止前不得接管新写。

Planner 只有受控只读工具；实际工具集合和执行隔离阻止项目写入、任意 shell 和脚本。核心 update_plan 保存完整草稿变化，最终候选通过 DAG、输入、验收覆盖、授权和问题校验；Planner 收尾后重新核对版本与来源，事务发布 Ready。默认在现有授权内自动执行；用户选“先审阅”才等待开始。

每个节点创建一个有限 pi Session，包含必要的多轮模型/工具循环；工具全部 sequential，完整 execute 进入受限 helper。控制提案接受后关闭入口、触发 abort，外层等待停止，不在工具内等待自身 idle。单节点与多节点采用同一条链。

实际可丢弃仓库验收：读取、修改、指定检查、diff、错误、停止、子进程退出。隔离未成立时只在隔离 VM/容器继续实验，不交付不受控宿主写入。模型密钥不能进入 renderer、shell 或导出。

### M2：亮点从这里接受检验

所有工具、产物和检查通过真实 Run 上下文归属节点。详情区区分动作成功、产物已保存、验证通过及验证覆盖；源码改了、旧 CI 绿灯晚到或输入无法确认时，历史保留，当前有效性改为过期/未知。

用户改目标/范围/必需条件产生 TaskRevision；步骤调整产生 PlanRevision。停止旧效果后，按节点语义、实际产物引用与可确认源输入计算影响。未知 shell 输入或共享文件影响保守扩大复核。发布新版使用旧计划/输入的 CAS，晚到旧事件不能更新当前图。

正常计划内的代码写入不引发每工具一次重规划。旧中间产物可以被后续节点有意替代；完成判断核对当前最终条件与累计补丁，不能要求所有中间快照永远不变，也不能由模型自行删除必需验收。

完成两类真实开发题：依赖升级中更改兼容约束；API/页面/测试小功能中改变一项验收。观察用户是否能用界面理解影响，记录保守复验造成的额外成本。

### M3：局部继续不等于回滚

先关闭该任务新效果入口、停止活动 Run，在当前 Task worktree 核对用户修改和未知效果。事务创建新 attempt 的同时，将所选节点及受影响下游标为待复核并撤销当前完成资格，然后才启动执行。过期首先触发重读/复验；不能盲目重放历史写操作，不能自动 reset、stash、clean 或覆盖人工修改。重做会影响哪些下游，先以预览说明；实际输入又变则重新核对。

加入 finite 的持久 Wait、预算/期限和启动/系统唤醒补查；同一任务一项 pending wakeup，重复事件合并。只有新信息、需要查证或合理重试才调用模型。once 不自动变成后台托管。维护模式仅显示最近有效输入的 healthy/unhealthy/unknown，不永久完成。

恢复次序为关闭旧效果 → 确认子进程停止 → 查证动作/工作区 → 刷新输入/证据 → 新 Run。无需重开旧 pi session。应用关闭/休眠期间不假装仍在运行。

若真实案例需要 CI，仅接一个指定平台的只读来源，验证仓库、提交、检查 ID。文档示例维护可用于验证第二次时间变化；不将它包装成另一项已证实市场。

### M4：用最强基线比较，决定继续投入什么

从已可访问产品选一到两个配置充分的基线：Codex 当前规划/长任务能力、Cursor Plan Mode、Devin 步骤视图、Kiro Specs＋可用验收集成；另外审查 OpenClaw Workboard 或 Mastra Factory 是否更适合直接扩展。不购买全套竞品，也不为比较搭通用平台。

完成本地打包、快照/产物一致导出与必要恢复说明。当前交付边界是本地补丁和报告；自动 push、PR、发布及通知他人不计作已实现。

## 4. 最小关键验收

以下是拟实施 oracle，不表示测试已存在或已通过。使用受控 provider/临时仓库覆盖状态与故障；真实 provider 只验证接线和实际任务。每项保留足以打破假设的最小断言，不给所有函数铺测试。

| ID | 触发 | 必须观察到 | 阶段 |
| --- | --- | --- | --- |
| T01 | Planner 尝试 write、shell、项目脚本；不完整或循环计划 | 项目无效果；具体错误回到草稿；缺失依赖/必要验收阻止 Ready | M1 |
| T02 | 计划 token 截断、重复/乱序草稿、提交后退出 | 不完整内容不执行；sequence/CAS 生效；有效候选查证后发布，无假历史 | M1 |
| T03 | UI 伪造 Ready、改范围、旧的开始决定晚到 | 核心拒绝；仅已发布且在授权内的当前计划可调度 | M1/M2 |
| T04 | 同一请求重试，首次 pi 消息前退出 | 单一接收记录；冲突内容拒绝；没有运行的步骤不显示执行成功 | M1 |
| T05 | 文件越界、符号链接、临时输出或派生进程绕路 | 全执行路径受限；Main 不跟随工具指定路径借权读取 | M1 |
| T06 | provider/shell 不理 abort，或 Main 被强杀 | 入口关闭；监督收尾；未确认退出不启动新写 | M1/M3 |
| T07 | 两个桌面/CLI；崩溃后旧 helper 可能仍活着 | 唯一 owner；epoch 拒绝旧请求；接管先查证旧效果停止 | M1/M3 |
| T08 | 控制提案置于工具批次首/末，混合 write；普通文本自称完成 | 合法提案后无新工具；前序效果收尾；无自等待死锁；文本不能创建状态 | M1 |
| T09 | 规划栏关闭期间仍推进/提出问题，重新打开、切图列表及重连 | 隐藏不改变运行和数据；主对话可作答；重开显示最新状态且保留选择；偏好可恢复，不重发 submit | M0/M2 |
| T10 | 运行中改兼容约束；同时有新版草稿与旧 Worker 事件 | 旧效果停止；影响预览匹配当前版本；旧事件只归历史 | M2 |
| T11 | 改一个输入，独立分析仍适用；另有未知 shell 依赖 | 明确引用的保留，受影响的复核；覆盖不明保守失效并解释 | M2 |
| T12 | 新代码出现后旧 CI pass 晚到；验证期间又改输入 | 旧证据不能完成当前节点/任务；缺源身份为 unknown | M2 |
| T13 | 模型改测试、删除计划中必需验收；验收后累计补丁再变 | 必需条件仍受保护；旧接受失效；有效替代不抹掉历史来源 | M2 |
| T14 | 局部继续前用户手工修改，下游仍活动或已有 pass | 停止活动 Run；原子登记新 attempt 并使受影响完成资格失效；保留人工修改，不重放补丁或还原整个目录 | M3 |
| T15 | 写成功但回执丢失；模型换 toolCallId 重试同一语义 | 相关写冻结并查证；不借新 ID 绕过 unknown | M3 |
| T16 | 突发事件、自身写入、休眠漏周期、来源不可达 | 合并补查；不递归重规划；显示观测缺口，不补跑每个模型周期 | M3 |
| T17 | 预算耗尽、反复无进展、取消/到期；持续重规划 | Planning/重规划/所有 attempts 均计入；停止新动作并清理，保留原因 | M3 |
| T18 | 保存产物/数据库失败，或最终检查前输入再变 | 无缺失引用的假成功；最终提交重新核对当前计划和来源 | M2/M3 |
| T19 | 1024 / 736 / 360 px 切换左栏、规划栏、主区视图和工具面板 | 规划始终在右侧，窄屏为右侧抽屉；各面板独立，关闭后焦点可返回；切视图不重跑任务 | M0/M1 |
| T20 | 中英切换、恢复语言偏好，同时任务有活动节点与未发送草稿 | 控件、动态状态、空状态和可访问名称覆盖两种语言；ID、版本、原始消息 / 输出、草稿和面板保持，未新增任务命令；模型回复语言不变 | M0/M1 |

每份证据保存实际版本、输入、来源、触发位置、回执与结果。通过只证明覆盖路径，不等于全部宿主环境安全或外部副作用恰好一次。

## 5. 同题比较与继续投入门槛

三个场景组：依赖升级中改兼容要求；API/页面/测试功能中改验收；隔夜恢复时外部代码或环境变化。任务本来一次能完成就让它完成，不人为等待。受控故障与模拟输入另表，不能代替自然使用收益。

固定代码起点、目标、资料、授权、工具链、预算和验收。记录产品版本、实际模型标识、推理配置的可见部分与用量缺口。同模型不可用时属于整产品比较，不称 harness 单变量实验。基线允许使用计划、记忆、项目规则、自动化及成熟模板；设置成本与后续复用成本分列。

| 指标 | 如何记录 | 避免的误读 |
| --- | --- | --- |
| 正确交付与证据有效性 | 当前输入上的固定检查＋必要人工审阅；统计过期完成误报 | 模型勾选或图变绿不等于正确 |
| 计划理解与修改 | 找出下一步、改变要求、定位受影响节点所需时间及准确性 | 图好看不等于减少工作 |
| 局部继续收益 | 保留的有效成果、重复分析/写入、必要和冗余复验分别记录 | 少跑节点但漏验不算收益 |
| 人工总分钟 | 设置、重述、查证、审阅、接管与幕后修复全计入 | 不只测最后一次点击到完成 |
| 用量与运行成本 | Planning/重规划/执行/总结分别计；等待与算力耗时分开 | 更强模型或更多预算的收益不归图或架构 |
| 稳定性与维护 | 丢改动、未知重复效果、错误过期判定；第二项目和上游升级投入 | 小样本不能外推长期成功率 |

早期投资门槛：三个真实任务至少两项降低人工总分钟；第三项无无法解释的明显退化；无验收集中已知的越权、丢修改、未知写重放或过期完成。它是个人项目的继续/缩小依据，不是统计显著的市场结论。若收益只在查看轨迹，缩小为轨迹/计划扩展；若成熟平台更少代码即可覆盖目标，优先扩展平台。

## 6. 学习与后续边界

M1 使用 runtime 实验 L01—L05、L07/L08 的 SDK 部分及 L13；M0/M2 使用 L11、L14/L15；M3 使用 L07/L08 恢复与 L16。热 steer、Harness operation 恢复、PTC、自动技能演进、多 Agent 均按真实需要后置，不作为画流程图的前提。

跨仓或多人协作出现后再研究资源锁与合并归属；关机仍需工作时先比较现成云宿主；第三方代码需要真正进程/权限隔离；远端写操作先补业务幂等与结果查询。当前方案更新不触发这些实现、依赖安装、真实模型调用或外部发布。
