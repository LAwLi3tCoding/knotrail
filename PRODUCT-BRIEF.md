# Knotrail（程迹）：产品方案 v3.1

> 交付更新（2026-09-06）：可运行的 v0.1.0 已实现。本文保留设计与研究背景，当前能力、限制和验收以 [README](README.md)、[实现架构](ARCHITECTURE.md) 与 [验证报告](docs/VALIDATION.md) 为准。
状态：待实施设计，2026-09-05。v3 取代 v2“项目托管 Agent”的主定位；前端从第一阶段开始，所有任务先规划再执行。本次界面修订以 Codex App 为主要参照，结合 DSH 和本地 Catdesk 的工作区布局；规划固定在可独立开关的右侧栏，App 支持简体中文和 English。当前基线由本文件、[交互设计](UI-DESIGN.md)、[详细架构](ARCHITECTURE.md)、[竞品对比](COMPARISON.md)、[实施计划](IMPLEMENTATION-PLAN.md) 组成。

## 1. 名字与定位

**Knotrail，中文名“程迹”。** 名字取节点连接与执行轨迹之意；“程迹”对应任务推进的过程与留下的成果。它是本项目采用的工作名称，不代表已经完成商标或域名可用性核验。

**Knotrail 是基于 pi 的桌面编码工作台：计划可以改，过程看得清，已有成果接得上。**

用户提出目标后，App 先把问题、依据和步骤整理成可查看的计划，再推进编码。计划中的每个节点都会关联实际操作、代码或文档产物、验证和待解决问题。工作被打断，或者用户中途改了要求，App 会展示影响和新的执行安排。

它应当能承担日常功能开发、修复和迁移。持续委托保留为时间维度的能力，用于等待环境、等待决定、隔天继续；不再用“后台项目管家”概括整个产品。

首批用户是需要亲自理解、修改并接管 Agent 工作的个人开发者或小团队技术负责人。优先验证已有代码库中需要分步骤完成、过程中可能调整要求的任务。人群规模、付费意愿及节省时间尚未验证。

## 2. 用户打开 App 会看到什么

App 的主要布局和操作习惯沿用 Codex App，并参考 DSH 的独立面板、活动投影和分组设置，以及本地 Catdesk 的项目任务导航和工具入口。左侧是可折叠项目与任务列表；中间提供对话 / 活动 / 变更视图和底部输入；文件 / 终端 / 预览在工具面板按需打开。输入区保留模型、权限、推理强度和上下文信息。**规划固定在右侧可独立开关的侧边栏，包含规划过程、任务图 / 列表和节点详情；窄屏从右侧抽屉展开，不移到下方。** 布局、来源与交互状态见 [UI-DESIGN.md](UI-DESIGN.md)。

| 区域 | 规划时 | 执行时与结束后 |
| --- | --- | --- |
| 规划侧边栏：过程 | 展示已观察的事实、约束、待答问题、方案摘要、草稿更新和依据链接 | 保留当前计划及修订记录；展示实际工作与原计划的变化 |
| 规划侧边栏：步骤 | 逐步形成节点、依赖、预期产物与验证方式；未定内容标记为草稿 | 显示每个节点的进展、阻塞、产物和验证有效性；图与列表使用同一份计划数据 |
| 节点详情 | 目标、输入、依据摘要、预计操作、预期产物、检查标准 | 实际操作、输出与 diff、检查结果及缺口、历次 attempt、重做原因 |
| 主对话与操作入口 | 补充信息、修改约束、提出替代方案 | 对选定节点提问、修改要求、暂停、继续或申请重做受影响工作 |

规划栏首次默认关闭，之后记住用户开关选择；关闭后对话区扩展，重新打开恢复所选视图和节点，并展示最新状态。开关只控制展示，不改变先规划后执行、不暂停任务、不取消记录，也不影响左侧导航和产物查看。必要决定在主对话同步显示，不因侧栏隐藏而丢失，也不强行弹开侧栏。

App 支持简体中文和 English。正式应用默认跟随系统语言，并保存手动偏好；界面语言与模型回复语言独立。切换界面语言保留当前任务、计划版本、节点选择、面板、输入草稿和进度，不重启任务或重发事件。代码、路径、命令、历史消息与工具原始输出保持原文，不翻译或改写任务 / 节点 / 事件 ID。

本轮原型用模拟数据展示这些操作。模型与权限配置只展示未接入状态，工具面板不执行真实命令、不访问真实文件或网页；页面参照不等于运行能力已经实现。M0 完成界面与状态切换，M1 接入真实规划执行，M2 关联活动、变更、产物和节点证据。

规划过程展示面向用户的进度和依据摘要，不声称呈现模型内部思维链。日志、命令和全文文件按需展开，默认先回答“这一步在解决什么、现在有什么结果、下一步需要什么”。

图不是模型写完一段 Mermaid 后附上的图片。模型生成结构化计划，Coordinator 校验并保存，App 根据同一份 Plan 数据渲染图和列表。计划改变与节点进展会更新这份可交互视图。Mermaid 可用于文档中的架构说明，不承担任务执行状态。

## 3. 每项任务都先规划

任务统一经过 **Planning → Ready → Executing → Verifying**。出现新事实或要求变化时，可返回规划并生成新版本；发生等待或失败时，保留当前节点、产物和原因。

- **Planning**：阅读项目、核对事实、明确约束与检查方式，生成计划。此阶段可调查和保存计划草稿，不进行计划中的源码修改。
- **Ready**：结构、依赖、输入与执行范围已校验，计划版本已保存，可以开始实施。它表示计划可执行，不表示每一项假设已经被事实证明。
- **Executing**：按已保存计划执行，在节点下记录实际动作和产物。新发现需要调整步骤时，留下修订说明与版本。
- **Verifying**：核对产物与约定条件，把实际结果、适用版本和未验证部分展示给用户。模型说“完成”不能直接点亮验证状态。

简单任务可以只有一个节点，复杂任务默认拆为 3—7 个用户能理解的工作节点。节点应代表有目标与产物的一段工作，不把每次工具调用拆成一格，也不强迫小修改写完整需求文档。

**先规划不等于增加逐步审批。** 用户原请求已授权的工作，Ready 后默认自动执行；用户可选择“先审阅计划”。只有缺少会改变结果的决定、范围扩大或需要额外权限时才停下来。自动执行和先审阅两种方式都展示完整计划。

用户修改目标、边界或完成条件时，产生新的任务版本与计划版本；仅细化步骤也产生计划版本，但不能借此扩大授权。规划页面必须区分“步骤调整”和“请求更多权限”。

## 4. 主打演示：改了要求，已有工作还能接上

首例选一个真实兼容依赖升级任务：

> 把依赖升级到指定版本，更新适配代码，保持当前公开接口，完成构建、测试和指定示例检查，交付可审查补丁。

初始计划可包含五个节点：核对升级影响 → 修改适配代码 → 更新受影响用法 → 验证接口和示例 → 整理交付。界面同时展示哪些节点可以独立进行，实际首版仍只有一条活动写运行。

执行到测试时，用户补充：“还要继续兼容旧版配置文件。”App 应完成以下流程：

1. 立即停止使用旧输入启动新效果，显示“正在停止”，收尾并查证正在进行的操作与当前工作区。
2. 规划栏展示新要求与原计划的差异，给出基于当前事实的影响预览和依据；仍适用的调研成果继续保留。
3. 保存新任务与新计划版本。兼容适配及相关测试需要更新；无法确认影响范围的部分标为待复核。
4. 在当前工作区启动新的 attempt，复用仍适用的输入与产物，修改受影响代码并重新验证。
5. 每个相关节点保留前后 attempt、差异和检查结果；旧验证可追溯，但不能证明新要求已满足。

这里的“局部重做”是在当前工作区继续修改和验证，**不承诺任意节点一键回到过去，也不自动撤销人工修改或外部副作用。** 影响判断先覆盖显式依赖、已声明输入和可核对的代码变化；关系未知时扩大复验，不能把推测画成确定事实。

第二个场景选择一个跨接口、页面和测试的小功能。它用于验证这个工作台能支持普通开发：用户能分别检查接口契约、实现 diff、页面产物和验证，再理解它们如何组成完整功能。文档示例持续维护后置，用于验证隔天变化后继续，而不是同时扩展另一类产品市场。

## 5. 真正要争取的亮点

规划模式、流式图和步骤详情已有成熟实现。Cursor 已提供流式 Mermaid 计划；Devin 可展开步骤活动；Kiro 有需求、设计、任务与测试关联。我们的机会需要落在这些能力组合带来的实际收益上。[Cursor](https://cursor.com/changelog/2-2)、[Devin](https://docs.devin.ai/work-with-devin/devin-session-tools)、[Kiro](https://kiro.dev/docs/specs/)

| 主打能力 | 用户得到什么 | 必须验证的差异 |
| --- | --- | --- |
| **计划 v1→v2 的影响预览** | 改要求时，看清需要重做哪些步骤、为什么，以及哪些成果可继续使用 | 比现有计划修改与重新执行少多少解释、人工查找和无效重做；未知影响不能漏报为安全 |
| **节点产物与当前验证关联** | 点开一步，看见具体产物、当前适用的检查和验证缺口 | 代码、条件或环境变化后，不把旧绿灯当新结果；也不靠更多日志增加审阅负担 |
| **中断与局部继续时查证成果** | 隔天回来，能理解已完成、未确认、已过期和待决定的部分，再继续 | 比重新读对话或整体重跑少多少接管时间，同时不损坏人工修改、不重复未知动作 |

这三项是相互支撑的产品行为，不是三个新框架。它们均为待验证假设，不能写成“业内只有我们有”。其他具体方向与优先级见 [DIRECTIONS.md](DIRECTIONS.md)；最强替代方案见 [COMPARISON.md](COMPARISON.md)。

## 6. 技术与产品边界

- **pi SDK 承担 Agent 运行。** 应用保存任务授权、计划版本、节点 attempt、观察、产物与检查关系；不重写 pi loop。
- **Coordinator 是状态写入的唯一入口。** 模型可提出计划与状态更新，校验、版本切换、动作准入和检查有效性由应用执行。
- **桌面从 M0 开始。** Electron 前端是产品交付的一部分；CLI 仅用于诊断与开发，不再采用先做 CLI、最后加前端的路线。
- **一项任务的工作区跨 attempt 保留。** 首版一个 Git 项目、一条活动写运行；图上的多个节点不等于多个 Agent 并行。
- **等待不占用持续模型循环。** 本地事件或恢复时检查触发新的有界运行；应用退出或机器睡眠期间暂停执行与观察，回来后显示间隔并核对当前事实。
- **DSH 提供扩展机制参考。** 首版只落实实际需要的 source/context/tool/check 贡献，不引入动态插件市场。pi AgentHarness 为专项实验，未成为生产前置依赖。

任务图、状态库和桌面面板都不是壁垒。产品是否值得长期维护，取决于用户在真实开发与变更中能否更快理解、修改和接管工作。

## 7. 实施与验证

| 阶段 | 用户可见的交付 | 粗估工程日 |
| --- | --- | --- |
| M0 | Codex / DSH / Catdesk 参考的双语工作区、独立右侧规划与工具面板、节点详情及计划修订演示 | 3—5 |
| M1 | 真实规划与执行：pi 接入，所有任务先规划再执行 | 6—9 |
| M2 | 节点证据、计划变化和影响预览 | 7—11 |
| M3 | 局部继续、中断恢复与持续委托 | 7—11 |
| M4 | 真实任务对照、问题修复与交付 | 5—8 |
| **合计** | **可验证的个人桌面首版** | **28—44** |

这是安排工作的初估，取代 v2 的 20—34 日；M0 后按交互反馈、M1 后按真实接线风险重估。首五日包含在总估算中，用于可交互演示和最薄技术验证，不承诺正式 Agent、可靠恢复或竞争优势已成立。

同题对照至少包括可实际使用的成熟编码产品，并优先覆盖 Kiro、Cursor 或 Codex 的正式规划工作流；另用 OpenClaw Workboard 或 Mastra Factory 检验是否只需扩展现有产品。统计人工理解与修改计划的时间、接管分钟、不必要重做、错误复用、当前有效的交付结果和总维护成本。

若既有产品加少量配置已提供相同收益，就缩小为 pi 工具或插件；不能仅因做出了图和桌面就认定产品成功。当前完成的是方案、来源核对和交互设计；应用能力、沙箱、恢复与竞品收益均未完成实测。
