# Knotrail（程迹）：桌面交互方案 v3.1

> 交付更新（2026-09-06）：可运行的 v0.1.0 已实现。本文保留设计与研究背景，当前能力、限制和验收以 [README](README.md)、[实现架构](ARCHITECTURE.md) 与 [验证报告](docs/VALIDATION.md) 为准。
状态：产品设计，2026-09-05。本次以 Codex App 为主要参照，结合 DSH 与用户本地 Catdesk 的页面布局，补齐工作视图与工具面板；规划侧栏固定在右边，App 支持简体中文和 English。本文定义拟实现的界面与行为；会话中的交互示意使用模拟数据，未连接 Agent、仓库或模型。产品范围以 [产品方案](PRODUCT-BRIEF.md) 为准，运行语义以 [架构](ARCHITECTURE.md) 为准，交付顺序见 [实施计划](IMPLEMENTATION-PLAN.md)。

## 1. 界面的工作对象

Knotrail 主要沿用 Codex App 的任务导航、对话工作区、工具活动和输入布局；计划过程、图和节点详情收进可选的右侧规划栏。用户随时能回答：现在在做哪一步，这一步为何需要做，实际得到了什么，哪些结果仍有效，接下来需要自己决定什么。

所有任务先规划再执行。规划期间可以在已有权限内读取项目、检索资料和收集事实，不可因生成了草稿就开始修改项目。完整计划须通过结构与权限检查，并持久化为可执行版本，才能启动执行。简单任务可以只有一个实施节点与其验收条件，不强制拆成大图。

原授权内、没有未决条件的计划进入 `ready` 后默认开始执行。用户可选择“先审阅计划”；权限扩大、需求语义不清或高影响选择才形成具体决策请求。“先规划”不等于每一步都需要用户批准。

## 2. 主工作区与右侧规划栏

主要布局、留白、任务切换、对话和输入习惯以 Codex App 为参照。DSH 与 Catdesk 补充项目导航、独立面板、工作视图和配置层级。保留 Knotrail 名字；工作区应能展开查看活动、变更与产物，不能只剩聊天气泡和计划列表。规划仍是可选右栏，不默认占满中央区域。模拟面板明确标识，未接入的模型或权限配置不得呈现为已生效。

上一版只采用分栏结构，未达到用户要求的 Codex 视觉接近度。本轮以 [Codex 官方页面](https://openai.com/codex/) 上实际查看的三张界面图校准：

- [导航参照](https://images.ctfassets.net/kftzwdyauwt9/UtVI8zlaSGX2kpwGW6pYH/d55ba8e59b2acf5758001feaf46a0449/Designed.png?w=3840&q=90&fm=webp)：原生窗口控件、紧凑图标导航、置顶与项目分组、平面任务行。
- [对话参照](https://images.ctfassets.net/kftzwdyauwt9/3VNkIvFSqReRhjyqqBUOjd/cea1689f7e9ce0215dd3699e7befd6b0/Built.png?w=3840&q=90&fm=webp)：右对齐用户气泡、无品牌标签的回复、紧凑工具记录和单行变更入口。
- [输入框参照](https://images.ctfassets.net/kftzwdyauwt9/4VICAqwJvjaSSJZpERHfXo/12fec864cec9d3fa3b6dd47dd8c5059b/codex-landing-product-surfaces-app.png?w=3840&q=90&fm=webp)：大圆角容器、轻边框与阴影、左下附件入口及右下黑色圆形发送按钮。

这些是官方公开示意，不是本机窗口截图；未证明与用户安装版本完全相同。用户提供截图后，以该截图为准。不能用文档功能描述推导界面外观，也不能将“浏览器没有报错”当作视觉还原通过。

补充参照及采用范围：

- **DSH**：固定参考提交 `d347e703908d0406b7a7ef80e3a0e594d86b2215`。采用 [AppFrame](https://github.com/deepseek-ai/deepseek-harness/blob/d347e703908d0406b7a7ef80e3a0e594d86b2215/packages/client/ui-layout/src/client/AppFrame.tsx) 的左右独立栏、[SidebarRoot](https://github.com/deepseek-ai/deepseek-harness/blob/d347e703908d0406b7a7ef80e3a0e594d86b2215/packages/client/ui-sidebar/src/client/SidebarRoot.tsx) 的导航层级，以及 [模型配置页](https://github.com/deepseek-ai/deepseek-harness/blob/d347e703908d0406b7a7ef80e3a0e594d86b2215/docs/user/guide/providers-models-page.png) 的分组设置。其 [Web Client 架构](https://github.com/deepseek-ai/deepseek-harness/blob/d347e703908d0406b7a7ef80e3a0e594d86b2215/docs/subsystems/web-client.md) 将聊天和轨迹作为同一事件源的不同投影，这一原则用于本产品的对话与活动视图。
- **Catdesk**：依据本轮实际查看的本地界面，参考项目分组任务、新任务、技能、定时任务与设置的导航，紧凑顶部工具入口、底部可开关终端、右侧文件树，以及输入区的权限、推理强度与上下文信息。只记录布局和操作关系，不复制其本地任务内容、路径或配置。

参考页面不代表继承其执行能力。当前模拟将文件、终端和预览集中在底部工具面板，右侧专用于规划；正式功能按 M0 展示、M1 执行接线、M2 证据关联推进，不扩展为插件市场或通用浏览器产品。

| 部分 | 本次视觉约束 |
| --- | --- |
| 窗口与导航 | 窗口控件与导航放在左侧；项目分组可折叠，任务使用平面列表和浅灰选中背景；设置位于稳定入口 |
| 主区顶部 | 使用紧凑任务标题、对话 / 活动 / 变更视图与独立工具入口，规划开关位于右侧；不增加大号产品标题或说明横幅 |
| 对话 | 用户气泡右对齐；回复为自然正文；工具活动按需折叠，减少重边框、分隔线与多余角色标签 |
| 输入 | 用大圆角多行输入区域与附件 / 发送控件；保留模型、权限、推理强度和上下文信息的位置，未接入的配置显示不可用原因 |
| 工具面板 | 主区底部可独立开关，包含文件、终端、预览；模拟文件树、输出和预览可切换查看，但不执行命令、读真实目录或访问页面 |
| 演示操作 | 用对话内的上下文操作推进模拟步骤，移除永久占据底边的演示工具栏；保持模拟标识可见 |
| 新增能力 | 规划侧栏的过程、步骤与节点证据保留，关闭后主区恢复；不借视觉复刻扩展真实产品能力范围 |


关闭规划栏：

```text
┌ 项目 / 任务 ─────┬ 任务标题 · 当前状态          [工具] [规划] ┐
│                 │                                          │
│ 当前任务        │       对话 / 活动 / 变更                  │
│                 │             产物 / 差异入口               │
│                 │             必要问题与决定               │
│                 │                                          │
│                 ├ 输入目标、补充要求、继续任务 ─────────────┤
│ 设置 / 语言     ├ 文件 / 终端 / 预览（按需打开）───────────┤
└─────────────────┴──────────────────────────────────────────┘
```

开启规划栏：

```text
┌ 项目 / 任务 ─────┬ 任务标题 · 当前状态          [工具] [规划] ┐
│                 ├ 对话 / 活动 / 变更 ───┬ 规划 ─────────────┤
│ 当前任务        │                       │ 过程 / 步骤      │
│                 │ 产物、问题和决定      │ 事实、草稿、修订 │
│                 │                       │ 计划图 / 列表    │
│                 │                       │ 节点详情与产物   │
│                 ├ 输入框 ───────────────┤                  │
│ 设置 / 语言     ├ 文件 / 终端 / 预览 ───┤                  │
└─────────────────┴───────────────────────┴──────────────────┘
```

- **独立开关**：标题栏右侧“规划”按钮控制右侧栏，与左侧导航、主区视图和底部工具面板独立。首次默认关闭；正式应用保存用户开关偏好，任务分别保留所选视图、节点和展开状态。打开文件或终端不能替换规划，关闭规划不能连带关闭工具面板。
- **仅控制展示**：关闭不暂停任务、不关闭先规划后执行、不删除过程或停止事件更新。主区占用释放的宽度；重新打开显示最新版本与进度，恢复仍存在的节点选择。
- **过程 / 步骤**：过程视图放事实、约束、候选方案、必要问题和计划修订；步骤视图放图 / 列表及所选节点的概览、产物、验证和历史。
- **必要决定始终可达**：问题在主对话中同步出现，侧栏用相同决定记录展示；任一入口回答后另一处更新。不因侧栏关闭而漏掉问题，不因新事件强制打开侧栏。

正式桌面先按参照中的导航比例调整，左侧任务栏约 220–240 px、规划栏约 300–360 px，中间弹性伸展；实际宽度以内容可读和用户指定截图为准。关闭规划栏后主区扩展。宽度不足时先收起项目导航；736 px 以下规划从右侧抽屉展开，关闭后回到主区，不能移到对话下方。抽屉具备关闭入口、遮罩和焦点管理；面板状态仍分别保留。窄屏优先列表，图中文字不随整图缩小。

视觉以 Codex 式中性灰白 / 石墨表面、系统字体、轻边框和克制的按钮为主；冷蓝只用于选中项与执行关系，状态同时使用文字。输入框属于主对话，不随规划栏隐藏。文档中的尺寸是设计值，不声称来自本机 Codex 测量。

### 2.1 简体中文与 English

正式应用首次跟随系统语言：中文使用简体中文，其他语言回退 English；设置提供「跟随系统 / 简体中文 / English」，保存手动偏好并在下次启动恢复。模型回复语言单独设置，切换界面语言不替用户修改任务要求或下一次模型输入。

导航、按钮、提示、状态、空状态、错误、日期数字格式、设置和可访问名称均进入两套文案。任务 ID、计划 / 节点 / 事件 ID、代码、路径、命令、用户消息、模型历史回复和工具原始输出保持原文。结构化状态码在展示时翻译，不覆盖原始记录；节点目标等任务内容也不因切换语言自动改写。

语言属于 UI preference。切换时保留任务、计划版本、当前节点、主区视图、展开面板、输入草稿及执行进度；不重启任务、不重新订阅以重放事件、不创建 Run 或重发命令。语言切换本身不触发模型调用。原型只验证两套界面文案与状态保留，持久化和模型回复语言设置须在正式应用中接线验证。

当前示意为节点说明预置中英两套示例文案，用于核对侧栏排版；这不代表真实任务内容会随界面语言自动翻译。历史对话、输入草稿和终端示例原文保持不变。为便于审阅工作区，示意首次展示终端输出面板，规划栏仍默认关闭。

## 3. 规划过程与规划结果

规划侧栏开启时展示以下工作记录；隐藏期间仍记录并更新，重新打开不从头重演：

| 事件 | 展示内容 | 产生者与限制 |
| --- | --- | --- |
| 收集事实 | 文件、接口、版本、来源与观察时间 | 工具事实有来源；模型推断明确标记 |
| 识别约束 | 用户要求、现有接口、权限和预算边界 | 引用约束来源，不把猜测变为授权 |
| 比较方案 | 简短方案、选择理由、代价与尚未确定的点 | 面向用户的方案摘要，不展示或模拟隐藏思维链 |
| 提出问题 | 待选事项、影响节点与具体选项 | 只有会改变范围、权限或结果的缺口才阻塞 |
| 更新草稿 | 节点、依赖、预期产物与检查条件 | 明示“草稿，不可执行”，不产生已完成状态 |
| 计划就绪 | 计划版本、基线、验收条件、授权范围 | 由协调器验证并落盘后发出 |
| 计划变化 | 新旧版本差异、变更原因、影响预览 | 草稿变化不覆盖已执行版本 |

流式展示按事件顺序逐条追加，允许展开来源与原文；不逐字播放大量推理文本。断线重连从已保存序号补齐并重读当前状态。事件重复不产生重复条目；观察缺口显示“此期间未观察项目”。

规划结束后，草稿转为计划 v1；相同节点在执行阶段继续显示状态与产物。不要在执行时重新生成一张看似相同却无法追溯的图。计划可以修订为 v2，但每次执行必须关联其实际使用的版本。

## 4. 计划图、执行轨迹与节点详情

计划图描述准备做的工作及依赖；执行轨迹描述实际发生的 NodeAttempt，即 `purpose=node` 的 Run。二者叠加显示，但不相互代替。一个节点可以有多次尝试，工具调用作为节点内部活动展开，不能把每一次 `read`、`grep` 都变成侧栏中的计划节点。

首版使用 3–7 个节点的典型任务；支持少量并列依赖，初期写执行可以串行调度。图上的并列表示依赖关系，不承诺同时执行。节点状态与列表状态共享同一读模型；不能由前端根据“模型说完成”自行画绿。

每个节点详情含以下内容，优先显示用户能审查的摘要：

| 字段 | 内容 |
| --- | --- |
| 目标 | 此节点要解决的问题、完成条件 |
| 输入与依据 | 上游产物、代码基线、相关约束、引用来源及有效性 |
| 计划动作 | 准备读取、修改和检查的范围 |
| 实际活动 | 已执行工具、状态、主要反馈；完整日志按需展开 |
| 产物 | 文件差异、文档、结构化发现、截图或检查报告；区分预期和已生成 |
| 验证 | 检查命令 / 判定者、覆盖范围、对应代码或产物版本、时间和结果 |
| 历史 | NodeAttempt 尝试、关联计划版本、重做原因、保留与失效记录 |

产物为空时显示“尚未生成”，不能提前展示预计路径为已生成文件。检查通过只说明其明确覆盖的条件；模型自评、固定命令结果和用户验收分开标识。查看历史版本时显示“历史结果”，避免用户把它当作当前证据。

## 5. 修改计划与局部继续

示例任务：将项目中的虚构依赖 `vector-kit` 从指定的 v1 升到 v2。原要求保留旧公开接口，执行到验证时用户新增“继续兼容旧版配置文件”。

1. 用户在主对话或规划栏提出“继续兼容旧版配置文件”。系统先停发该任务的新动作，等待当前动作安全收束；尚不能证明已停止时显示“正在停止”，不提前开放重做。
2. 生成 v2 草案和影响预览：调研结论可复用；兼容要求更新；配置适配与相关测试待重做；未执行的文档、交付节点使用新输入。
3. 影响基于声明的输入、产物与实际变化检测。无法证明未受影响的结果标记为“需要重新检查”，不可凭计划图上的连线声称精确影响分析。
4. 用户确认新需求后，协调器提交新版本。若用户已经明确表达完整新要求，确认可以是该指令本身；额外确认仅用于仍待选择的范围或权限变化。示意中的“应用变更”用于展示这个提交边界。
5. 确认活动 Run 已停止后，在同一事务中使选定节点和受影响下游的当前完成资格进入待复核状态，再开始新的 NodeAttempt；保留有效上游结果和全部历史。旧补丁是否保留由当前工作区事实决定，不自动抹除。

“局部继续”是根据当前事实重新执行指定范围，**不是回滚时间**。首次执行的外部副作用或不明结果不能靠重跑消除。界面须说明会复用、会重做和待核实的部分；对不能安全重复的操作，先核对实际结果。

## 6. 状态文案和异常

| 状态 | 用户看到的文案 | 允许的下一步 |
| --- | --- | --- |
| 草稿 | 规划中 · 尚未执行 | 补充要求、查看事实、停止规划 |
| ready | 计划已就绪 | 原授权内自动执行，或按“先审阅”设置等待 |
| running | 正在适配接口 | 查看活动、补充要求、请求暂停 |
| waiting_user | 需要决定：是否保留旧接口 | 首版暂停该任务，回答后继续 |
| waiting_external | 等待检查结果 · 下次检查时间 | 查看等待条件、手动刷新、暂停 |
| stopping | 正在停止 · 等待当前动作结束 | 保持只读，直到确认停止 |
| failed | 检查失败 · 见失败项 | 查看证据，修复或重试明确范围 |
| unknown | 结果待核实 · 未确认动作是否完成 | 核对实际结果；禁用可能重复写入的重试 |
| stale | 需要重新验证 · 输入已变化 | 查看变化原因、重新检查 |
| interrupted | 执行中断 · 已保留现有产物 | 核对状态后继续 |
| skipped | 本次不执行 · 原因 | 查看所属计划版本与替代节点 |
| completed | 已完成 · 对应版本及验收记录 | 查看节点产物与验证；后续变化另行标注 |

节点完成数仅表示当前计划内的工作项，不能宣称是精确工期进度。重规划后分母可能变化，应标注新计划版本；避免进度百分比假装单调增长。

## 7. 可访问性与数据边界

规划开关使用原生按钮和 aria-expanded/aria-controls；关闭时隐藏内容不进入 Tab 顺序，若焦点原在栏内则返回开关。窄屏规划抽屉开启时焦点移入，Tab 不进入遮挡的背景，Escape 或关闭按钮退出并返回开关。切换显示只改变 UI preference，不发送暂停/取消/执行命令。所有节点、视图切换、决策和继续操作使用原生按钮；Tab 顺序遵循界面阅读顺序，保留可见键盘焦点。图提供等价列表，必需内容不依赖悬停。触屏有效目标约 44 px；窄屏换行而非缩小文字。动态状态使用克制的 `aria-live` 通知，不朗读每个流式字符；尊重减少动态效果设置，不使用持续脉冲动画。

前端展示协调器提供的读模型。计划 / NodeAttempt / 验证记录的 ID 和版本必须贯穿事件及详情；前端选择节点、切换图列表和展开产物不改变执行状态。实际修改通过有类型的命令提交并验证当前版本，不接受前端直接写“完成”。

日志和文件内容按不可信文本渲染，不执行输出 HTML 或命令。凭据不进入前端读模型；过长输出有截断标记与完整记录入口。节点来源说明其观察范围，不能把“没有收到事件”显示为“项目没有变化”。

## 8. 交互示意与实现验收

本轮示意范围：Codex 式主对话、DSH 与 Catdesk 参考的导航和独立面板、对话 / 活动 / 变更视图、文件 / 终端 / 预览模拟面板、中文 / English 界面，以及原有右侧规划、节点产物、计划变更和局部继续。规划栏默认关闭，隐藏时能继续推进并在主对话回答问题；重新打开保留视图、节点选择和展开状态。正式应用的偏好保存要求见第 2 节；示意的当前状态以交互检查为准，不作为跨重载持久化证据。演示通过对话内操作推进模拟阶段，以便审阅每个中间状态；真实产品默认在原授权内自动执行。示意没有真实 shell、API、模型或权限执行，其通过交互检查不代表 Agent 运行机制已通过验证。

实现必须以真实任务验证：计划落盘前写工具无法执行；节点详情与实际工具 / 产物 / 验证记录一致；变化使旧证据失效；重试保留历史且不会盲目重复副作用；关闭侧栏不影响任务或必需决定，重开和重连后显示同一最新状态并保留有效选择，不重复提交；1024 / 736 / 360 px 下内容可读，键盘可完成关键操作，深浅主题均可辨识。

本轮新增验收：三个宽度下规划始终位于右侧，窄屏为右侧抽屉；左右侧栏、工具面板和主区视图可独立切换；中英文覆盖可见控件、动态状态、空状态和可访问名称。语言切换前后任务 / 计划 / 节点 / 事件 ID、输入草稿、已开面板和原始消息输出保持一致，不额外产生任务命令。M0 先验模拟交互，M1 验真实执行接线，M2 再核对活动、变更与节点证据的一致性。

视觉验收单独进行：在相同宽度下查看参照与实现，逐项检查窗口控件、导航层级、列表形态、消息对齐与行距、输入容器及按钮。记录公开参照与用户版本的差异；未取得用户版本截图时，不标记为像素级一致。行为验收仍使用上述开关、隐藏更新和节点检查，两类证据不得互相替代。
